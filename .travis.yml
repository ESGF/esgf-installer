sudo: required
env:
  matrix:
  - OS_TYPE=centos OS_VERSION=6

services:
  - docker


before_install:
  # - pwd
  # - ls
  # - sudo apt-get update
  # - sudo service docker restart
  - sleep 5
  - docker build -t esgf_install .


script:
  - pwd
  - ls
  - docker ps
  - docker images
  - docker run --name esgf_install -it -d esgf_install /bin/bash
  - sleep 5
  - docker exec -it esgf_install ./esg-autoinstall
