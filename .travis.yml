sudo: required
env:
  matrix:
  - OS_TYPE=centos OS_VERSION=6

services:
  - docker


before_install:
  - sudo apt-get update
  - sudo service docker restart
  - sleep 5
  - sudo docker pull centos:centos${OS_VERSION}


script:
 # Run tests in Container
# - cat /etc/*-release
# - pwd
# - ls
  - docker run centos:centos6 /bin/bash -c "cat /etc/*-release"
  - pwd
